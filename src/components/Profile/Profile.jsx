import {
  Flex,
  Input,
  Button,
  InputGroup,
  InputLeftElement,
  FormControl,
  Box,
  Text,
  Link
} from '@chakra-ui/react';
import SideBar from './SideBar';

const Profile = () => {
  return (
    <Flex>
      <SideBar />
    </Flex>
  );
};

export default Profile;
